/** Palette and motion used by the EVLV site (silver / graphite / silver). */
export const AURORA_DEFAULTS = Object.freeze({
  colorStops: Object.freeze(['#727572', '#181819', '#919093']),
  amplitude: 1.0,
  blend: 0.5,
  speed: 0.5,
});
