/**
 * The three "Join EVLV" cards. `type` matches a key in REGISTRATION_TYPES
 * (shared/registration.js); `variant` controls the card's visual treatment.
 */
export const JOIN_OPTIONS = [
  { type: 'Brand', prefix: 'JOIN AS A', label: 'BRAND', variant: 'dark' },
  { type: 'Talent', prefix: 'JOIN AS A', label: 'TALENT', variant: 'light' },
  { type: 'Agency', prefix: 'JOIN AS AN', label: 'AGENCY', variant: 'dark' },
];
